# ======================================================================
# CHANGEME: Configuration File Template
# ----------------------------------------------------------------------
# This is a generic template. Rename it to match your project's config format
# (e.g., config.toml, config.yaml, config.json) and update the content.
# ======================================================================

# Example TOML configuration
[general]
# Log level: debug, info, warn, error
log-level = "info"

[network]
# Port to listen on
port = 9000
# Network ID (1 = Mainnet)
network-id = 1

[storage]
# Data directory
data-dir = "/var/lib/ethereum/CHANGEME"
