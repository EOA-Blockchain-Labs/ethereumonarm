#!/bin/bash
# =============================================================================
# staking-monitor prerm
# Notes about user data preservation before removal.
# Does NOT remove /home/ethereum/.obol-monitor or .validator-monitor
# to preserve user configuration and lock files.
# =============================================================================

set -e

echo "Removing staking-monitor package files."
echo "User data preserved in:"
echo "  /home/ethereum/.obol-monitor"
echo "  /home/ethereum/.validator-monitor"
echo ""
echo "If crontab entries exist, remove them manually:"
echo "  sudo -u ethereum crontab -e"
